@model List<Bros.DataModel.Post>
@{
    
    Layout = "~/Views/Layouts/_ProfileLayout.cshtml";
}

@Html.Partial("_StatusUpdateForm")

@if (Model != null)
{
    foreach (Bros.DataModel.Post post in Model)
    {
        Html.RenderPartial("_NewsFeedPartial", post);
    }
}