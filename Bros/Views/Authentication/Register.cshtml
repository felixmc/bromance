@using Bros.Models
@model RegisterModel
@{
    Layout = null;
    ViewBag.Title = "Sign-Up Here!";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div>


        <h2>Username</h2>
        @Html.ValidationSummary()
    @using (Html.BeginForm("UserForm","Authentication", FormMethod.Post, new {enctype="multipart/form-data"}))
    {       
        <p>Email: @Html.TextBoxFor(x => x.Email)</p>
        <p>Password: @Html.PasswordFor(x => x.Password)</p>
        <p>Confirm Password: @Html.PasswordFor(x => x.ConfirmPassword)</p> 
        <p>First Name: @Html.TextBoxFor(x => x.FirstName)</p>
        <p>Last Name: @Html.TextBoxFor(x => x.LastName)</p>
        <p>Birthdate: <input type="date" name ="Birthdate" /></p>
		<p>Gender: @Html.DropDownList("Gender", new SelectList(Enum.GetValues(typeof(Bros.Enums.Gender))))</p>
        <p>Zipcode: @Html.TextBoxFor(x => x.Zipcode)</p>
        <input type="submit" value="Submit Product" class="my-button"/>
        
    }
    </div>
</body>
</html>
