
@if (!ViewBag.IsMuted)
{
	using(Html.BeginForm("PostStatus", "Content", FormMethod.Post))
	{
		<textarea name="status" placeholder="Enter status update"></textarea>
		<input type="submit" value="Post" />
	}
}