@model Bros.DataModel.Photo

@{
    var base64 = "";

    base64 = Convert.ToBase64String(Model.ImageData);

     
    var imgSrc = String.Format("data:image/jpg;base64,{0}", base64);
}

<img src=@imgSrc />